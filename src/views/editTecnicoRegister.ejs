<!doctype html>
<html lang="en">
<head>
    <link rel="shortcut icon" href="/img/favicon.png">
    <title>tecnicos informacion redline</title>
    <link rel="icon" href="/imgs/logo-white.ico">
    <link rel="stylesheet" href="/css/styleRegister.css">
    <link rel="stylesheet" href="/css/styleLogin.css">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.1/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-+0n0xVW2eSR5OomGNYDnhzAbDsOXxcvSN1TPprVMTNDbiYZCxYbOOl7+AMvyTG2x" crossorigin="anonymous">
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/css/styleNavar.css">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Nunito+Sans:ital@1&display=swap" rel="stylesheet">
    <link href='https://unpkg.com/boxicons@2.0.9/css/boxicons.min.css' rel='stylesheet'>
    <script>import 'boxicons';</script>
    <script src="https://unpkg.com/boxicons@2.0.9/dist/boxicons.js"></script>
</head>
</head>
<body class="login" >
    <%- include('partials/nav2.ejs') %>

<div class="container" style="margin-top: 150px; ">
    <div class="row justify-content-center mt-5 ">
        <div class="col-lg-5 col-md-8 col-sm-8">
            <div class="card">
                <%if (data){ %>
                <div class="card-title text-center border-bottom">
                    <h2 class="p-3"></h2>
                </div>
                <div class="card-body">
                    <form action="/registerTecnicoSend" method="POST">
                        <input type="hidden" name="id" value="<%= data.id %> " hidden>
                        <div class="mb-3">
                            <h5 for="usuario">Nombre</h5>
                            <input type="text" class="form-control" value="<%= data.miembro %>" name="nombre">
                        </div>
                        <div class="mb-3">
                            <h5 for="usuario">Correo electronico</h5>
                            <input type="text" class="form-control" value="<%= data.correo %>" name="correo" required>
                        </div>
                        <div class="mb-3">
                            <h5 for="usuario">Numero de cedula</h5>
                            <input type="text" class="form-control" value="<%= data.cc %>" name="cedula" required>
                        </div>
                        <div class="mb-3">
                            <h5 for="usuario">Numero de Celular</h5>
                            <input type="text" class="form-control" value="<%= data.celular %>" name="celular" required>
                        </div>
                        <% } %>
                        <div class="d-grid">
                            <a  style="font-size: 14px;" onclick="alert()" class="btn btn-primary">editar tecnico</a>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>
<script src="//cdn.jsdelivr.net/npm/sweetalert2@11"></script>
<script src="https://cdn.jsdelivr.net/npm/sweetalert2@11.1.5/dist/sweetalert2.all.min.js"></script>
<script>
  function alert() {
    Swal.fire({
      title: '¿Quieres actualizar los datos de este tecnico?',
      icon: 'warning',
      showCancelButton: true,
      cancelButtonText: 'NO',
      cancelButtonColor: '#d33' ,
      confirmButtonColor:'#3085d6' ,
      confirmButtonText: ` SI `,
    }).then((result) => {
      /* Read more about isConfirmed, isDenied below */
      if (result.isConfirmed) {
        Swal.fire('Tecnico actualizado exitosamente', '', 'success')
        const value = true
        setTimeout(timeUot, 2000, value);
      } 
    })
  }
  function timeUot (values){
    if(values){
      document.forms[0].submit()
    }
    
  }
</script>
    <script>
        var inputs = $("input[type=text]");
      for(var i = 0; i < inputs.length; i++){
        var aux = $(inputs[i]).val().trim();
        $(inputs[i]).val(aux);
      }
      </script>

<script src="//cdn.jsdelivr.net/npm/sweetalert2@10"></script>
<script src="https://ajax.googleapis.com/ajax/libs/jquery/3.3.1/jquery.min.js"></script>
<%- include('partials/footer2.ejs') %>
</body>
</html>